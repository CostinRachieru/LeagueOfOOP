System.out.println(gameInput.getMapHeight() + " " +
                gameInput.getMapWidth());
        for (int i = 0; i < gameInput.getMapHeight(); ++i) {
            System.out.println(gameInput.getMap().get(i));
        }
        System.out.println(gameInput.getNrHeros());
        for (int i = 0; i < gameInput.getNrHeros(); ++i) {
            System.out.print(gameInput.getPlayersType().get(i) + " ");
            System.out.println(gameInput.getPlayersPosition().get(i).get(0) + " " +
                    gameInput.getPlayersPosition().get(i).get(1));
        }
        System.out.println(gameInput.getNrRounds());
        for (int i = 0; i < gameInput.getNrRounds(); ++i) {
            System.out.println(gameInput.getMoves().get(i));
        }


        ArrayList<Hero> Heros = new ArrayList<>();
        HeroFactory heroFactory = HeroFactory.getInstance();

        Heros.add(heroFactory.createHero("Wizard"));
        Heros.add(heroFactory.createHero("Rogue"));
        Heros.add(heroFactory.createHero("Knight"));
        Heros.add(heroFactory.createHero("Pyromancer"));

        for (int i = 0; i < Heros.size(); ++i) {
            for (int j = 0; j < Heros.size(); ++j) {
                Heros.get(i).isAttackedBy(Heros.get(j));
            }
            System.out.println();
        }